# http

High-performance, allocation-conscious HTTP protocol primitives for Zig 0.16.0.

The library is transport-agnostic: networking, TLS, event loops, and application routing stay outside the protocol core.
